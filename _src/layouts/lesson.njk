<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="{{ 'https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.min.js' | url }}" integrity="sha384-Atwg2Pkwv9vp0ygtn1JAojH0nYbwNJLPhwyoVbhoPwBhjQPR5VtM2+xf0Uwh9KtT" crossorigin="anonymous"></script>
    <link href="{{ 'https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css' | url }}" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="{{ '/_src/styles/fonts.css' | url }}">
    <link rel="stylesheet" href="{{ '/_src/styles/main.css' | url }}">
    <link rel="stylesheet" href="{{ '/_src/styles/code.css' | url }}">

    <title>{{ title }} | Henry Prep Course </title>
  </head>

  <body>
    <div class="reader-bar-start">

      <div id="navbarContainer">
        <div id="navbarContained">
          <div id="logoHenry">
            <img src="{{ '/_src/assets/logo-white.png' | url }}" alt='logo Henry' height=27px class="d-inline-block align-text-top">
          </div>
          <div>
            <span id="lessonTitle">{{ title }}</span> | <span>Prep Course</span>
          </div>
        </div>
      </div>

      <div id="headerResponsive">
        <div id="logoHenry">
          <img src="{{ '/_src/assets/logo-white.png' | url }}" alt='logo Henry' height=22px class="d-inline-block align-text-top">
        </div>
        <div>
          <span id="lessonTitle">{{ title }}</span>
        </div>
      </div>

      <div id='main'>
        <div class="topnav" id='myTopnav' >
          {# Navigation #}
          {{ collections.all | eleventyNavigation | bootstrapNav({
            listChildItemClass: "dropdown-menu shadow", 
            activeKey: eleventyNavigation.key,
            listItemClass: "link",
            activeListItemClass: "activeLink"
          }) | safe }}
        </div>

        <div id='sidebar'>
          <div id="tocTitle">
            Contenido de la clase
            </div>
          <div id="tocBox">
            {# Table of contents #}
            {{ content | toc | safe }}
          </div>
          <div>
            {{ homework }}
          </div>
        </div>

        <div id='lesson'>
          <div style="display:flex; justify-content: right; right: 0; width: 100%;">
            <div id="readingTime">
                    Tiempo de lectura {{ content | readingTime }}
            </div>
          </div>
          <div>
            {# Lesson (markdown content) #}
            {{ content | safe }}
          </div>
        </div>

      </div>

      <div class="feedback">
        <a href='https://airtable.com/shrSzEYT4idEFGB8d?prefill_clase={{feedbackID}}' target="_blank">
              Dejanos tu feedback!👍
            </a>
      </div>

      <div class="footerContainer">
        <div class="footerContained">
          <p>Hecho con 💛 por alumnos de Henry</p>
        </div>
      </div>

      <div id="readerBar2">
        {% readerBar "3px",
        "black",
        "yellow" %}
      </div>

    </body>
  </html>